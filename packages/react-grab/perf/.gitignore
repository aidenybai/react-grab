*.json
!baseline-latest.json
!stage-*.json
*.log

# Per-run perf bench outputs written by e2e/perf-bench.spec.ts → PERF_LABEL.
# `baseline/` is committed (see !baseline/ below); other labels are local.
current/
!baseline/
!baseline/*.json
