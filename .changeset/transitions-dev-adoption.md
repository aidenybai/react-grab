---
"react-grab": patch
---

Add transitions-dev animations to toolbar UI: notification-badge slide-in + digit pop-in for the unread comment count, and an icon-swap cross-fade on the comments dropdown's "Copy → ✓" feedback. All transitions honor `prefers-reduced-motion: reduce`.
