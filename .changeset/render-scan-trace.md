---
"react-grab": minor
---

Add a render scan that highlights re-rendering components on a canvas overlay, and copies an agent-readable performance trace (per-component render cost + long-animation-frames) to the clipboard when you stop scanning. The toolbar shows a "Copied" pill on stop, and the scan state persists across reloads via sessionStorage.
