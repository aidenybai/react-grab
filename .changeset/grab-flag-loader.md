---
"react-grab": patch
---

Add `react-grab/vite` plugin and `react-grab/next` component that inject a loader script gated behind a `?grab=1` / `?grab=true` URL flag, so React Grab can be enabled on demand in both dev and production environments.
